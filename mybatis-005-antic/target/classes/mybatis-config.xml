<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <properties resource="jdbc.properties"></properties>
    <settings>
        <setting name="logImpl" value="STDOUT_LOGGING"/>
        <!--  是否开启驼峰命名自动映射-->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
        <!--延迟加载修改全局的配置setting，lazyLoadingEnabled=true，如果开启全局延迟加载，想让某个sql不使用延迟加载：fetchType="eager"-->
        <setting name="lazyLoadingEnabled" value="true"/>
    </settings>
      <typeAliases>
               <!--  &lt;!&ndash; 起别名第一种方式：typeAlias&ndash;&gt;
            &lt;!&ndash;        <typeAlias type="com.powernode.mybatis.pojo.Car" alias="Car"></typeAlias>&ndash;&gt;
            &lt;!&ndash;第二种方式：package&ndash;&gt;-->
        <!--  如果一个包下的类太多，每个类都要起别名，会导致typeAlias标签配置较多
        ，所以mybatis用提供package的配置方式，只需要指定包名，该包下的所有类都自动起别名，别名就是简类名。并且别名不区分大小写。-->
            <package name="com.powernode.mybatis.pojo"></package>
        </typeAliases>

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
                <!--最大连接数-->
                <property name="poolMaximumActiveConnections" value="3"/>
                <!--这是一个底层设置，如果获取连接花费了相当长的时间，连接池会打印状态日志并重新尝试获取一个连接（避免在误配置的情况下一直失败且不打印日志），默认值：20000 毫秒（即 20 秒）。-->
                <property name="poolTimeToWait" value="20000"/>
                <!--强行回归池的时间-->
                <property name="poolMaximumCheckoutTime" value="20000"/>
                <!--最多空闲数量-->
                <property name="poolMaximumIdleConnections" value="1"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <!--方式一sql映射文件创建好之后，需要将该文件路径配置到这里-->
        <!--  <mapper resource="CarMapper.xml"/>-->
        <!--  方式二 将整个包导入需要在resources创建同样的包名-->
        <!--<package name="com/powernode/mybatis/mapper"/>-->
        <!--    <mapper class="com.powernode.mybatis.mapper.CarMapper"/>
            <mapper class="com.powernode.mybatis.mapper.TUserMapper"/>-->
        <!--   <mapper resource="CarMapper.xml"/>
           <mapper resource="TUserMapper.xml"/>-->
        <!--   <mapper class="com.powernode.mybatis.mapper.CarMapper"/>
             <mapper class="com.powernode.mybatis.mapper.TUserMapper"/>-->
        <!-- 将包内的映射器接口实现全部注册为映射器 -->
        <package name="com.powernode.mybatis.mapper"/>
    </mappers>
</configuration>